// ===============================================
// 課題06: 最年長者を探す
// ===============================================
// 「名前」と「年齢」を持つ人の配列から、いちばん年上の人を探します。
//
// 実行方法:
//   npx tsx src/06-oldest.ts
//
// 学ぶこと:
//   - 配列 + オブジェクト … 1人＝オブジェクト、それを並べた配列
//   - 比較 … 年齢どうしを比べて、年上を選び続ける
//   （課題04の「最大値」と考え方は同じ。比べる対象が age になっただけ）
// ===============================================

const people = [
  { name: "田中", age: 20 },
  { name: "佐藤", age: 35 },
  { name: "鈴木", age: 28 },
];

// 「今のところ一番年上の人」を覚えておく変数。
// とりあえず先頭の人を入れておく。
let oldest = people[0];

for (let i = 0; i < people.length; i++) {
  // people[i].age で「i番目の人の年齢」を取り出せる
  if (people[i].age > oldest.age) {
    oldest = people[i];
  }
}

console.log(`最年長は ${oldest.name} さん（${oldest.age}歳）`);

// -----------------------------------------------
// 🔎 デバッガで見てみよう
//   oldest が「田中」から始まり、佐藤の行で切り替わる様子を見よう。
//   oldest は数値ではなく「オブジェクトまるごと」が入っている点に注目。
//
// 🚀 発展
//   - いちばん若い人を探す
//   - people に自分や友達を追加しても正しく動くか試す
//   - 年齢順に並べ替える（ここから先は sort という道具を使う）
// -----------------------------------------------
