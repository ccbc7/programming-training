# プログラミング練習


目的

プログラミングの基礎的な考え方を学び、。課題を解決するための見方・考え方を身につける。

---



## セットアップ手順
```bash
npm install
```

---

## 実行方法

どの課題も `npx tsx` で実行します。

```bash
npx tsx src/01-fizzbuzz.ts
```

ファイル名を変えれば、どの課題も同じように実行できます。

---

## 課題一覧

| No. | ファイル | 内容 | 主に学ぶこと |
| --- | --- | --- | --- |
| 01 | `src/01-fizzbuzz.ts` | FizzBuzz | `for` / `if` / `%` |
| 02 | `src/02-quiz.ts` | クイズゲーム | 入力 / `if` / 変数 |
| 03 | `src/03-price.ts` | テイクアウト・イートイン価格計算 | 引数 / 計算 / 途中変数 |
| 04 | `src/04-array.ts` | 配列の合計・最大値 | 配列 / `for` / `if` |
| 05 | `src/05-name-search.ts` | 名前検索 | 配列探索 |
| 06 | `src/06-oldest.ts` | 最年長者を探す | 配列 + オブジェクト / 比較 |
| 07 | `src/07-golf.ts` | **最終課題** ゴルフゲーム | 状態管理 / 変数 / `if` / オブジェクト / 配列 |

各ファイルの先頭に「やること」と「学ぶこと」、末尾に「🔎 デバッガで見てみよう」と「🚀 発展」を書いています。

---

## VS Code のデバッガを使う

1. `src/` の中のファイルを開く（例: `01-fizzbuzz.ts`）
2. 行番号の左をクリックして **ブレークポイント（赤い丸）** を置く
3. **F5** を押す
4. 赤い丸の行で実行が止まり、その時点の変数の中身が見える
5. もう一度 F5（または ▶ボタン）で次へ進む

例えば FizzBuzz の `for` の行で止めると、`i = 1`, `i = 2`, `i = 3` …… と
`i` が変化していく様子を目で追えます。

> 設定は `.vscode/launch.json` に入っています。開いているファイルがそのまま実行されます。

---

## TypeScript の導入タイミング
まずはバニラJSで動くようにする。TS化は完成したJSをAIにTS変換してみて観察する流れにするとわかりやすいです。

---