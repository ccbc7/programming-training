// ===============================================
// 課題02: クイズゲーム
// ===============================================
// 画面に問題を出して、キーボードから答えの番号を入力してもらいます。
// 正解したら score（点数）を 1 増やします。
//
// 実行方法:
//   npx tsx src/02-quiz.ts
//
// 学ぶこと:
//   - 入力 … rl.question(...) でキーボードからの入力を受け取る
//   - if   … 入力が正解と同じかどうかで処理を変える
//   - 変数 … score に点数をためていく
// ===============================================

// ↓ この2行は「キーボード入力を受け取る準備」。今は おまじない としてそのままでOK。
import * as readline from "node:readline/promises";
const rl = readline.createInterface({ input: process.stdin, output: process.stdout });

// TODO(1): 点数を入れておく変数を作る。最初は 0 点。
//   let score = 0;

// --- 第1問（ここはお手本。これを見て第2問を自分で作ろう）---
console.log("Q1. 日本の首都は？");
console.log("  1. 大阪");
console.log("  2. 東京");
console.log("  3. 福岡");
const answer1 = await rl.question("番号を入力: "); // 入力は「文字」で返ってくる

// TODO(2): answer1 が正解("2")かどうかで処理を分ける。
//   正解なら「正解！」と表示して score を 1 増やす。
//   ヒント:
//     if (answer1 === "2") {
//       ...
//       score = score + 1;
//     } else {
//       ...
//     }
//   ⚠️ 入力は文字なので、2 ではなく "2"（クォート付き）で比べること。

// --- 第2問（自分で作ってみよう）---
// TODO(3): 好きな問題を1つ出して、第1問と同じように正解判定する。

// --- 結果発表 ---
// TODO(4): 最後に score を表示する。
//   console.log(`あなたのスコアは ${score} 点でした`);

rl.close(); // 入力の受け付けを終了（おまじない）

// 困ったら answer/02-quiz.ts に答えがあります。
